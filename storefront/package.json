{
  "name": "@ecwid-product-bundles/storefront",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "node ../scripts/generate-storefront-loader.mjs && tsc --noEmit && vite build && node ../scripts/generate-storefront-loader.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
